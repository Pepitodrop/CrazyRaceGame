[package]
name = "crazy-race-game"
version = "0.1.0"
edition = "2021"
description = "A Dockerized 1v1 race game powered by Rust, R, TrumpScript and Piet"
license = "MIT"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
